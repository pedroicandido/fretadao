const initialState = {
  currentPoint: {},
  historyPoints: [],
};

const pointReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default pointReducer;
